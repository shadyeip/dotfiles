[core]
    editor = vim
    excludesfile = ~/.gitignore_global

[init]
    defaultBranch = main

[pull]
    rebase = true

[alias]
    st = status
    co = checkout
    br = branch
    lg = log --oneline --graph --decorate --all

[include]
    path = ~/.gitconfig.local
